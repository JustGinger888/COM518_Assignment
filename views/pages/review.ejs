<div class="container">
  <form onsubmit="ajaxReview()" id="poiAdd">
    <div class="form-group">

      Description:
      <input class="form-control mr-sm-1 mb-3" type="text" id="id" required autocomplete="off"
        placeholder="Enter an id">
      <textarea name="description" id="description" class="form-control" rows="3"
        placeholder="Place Of Interest Description" required></textarea>
    </div>
    <button class="btn btn-outline-primary w-100" type="submit">Submit</button>
  </form>
</div>
<ul id="results"></ul>
<script>
  document
    .getElementById("poiAdd")
    .addEventListener("submit", function (event) {
      event.preventDefault();
    });

  async function ajaxReview() {
    const body = {
      id: document.getElementById("id").value,
      description: document.getElementById("description").value,
    };
    const response = await fetch("/poi/review", {
      method: "POST",
      headers: {
        "Content-type": "application/json"
      },
      body: JSON.stringify(body),
    });
  }
</script>

</html>